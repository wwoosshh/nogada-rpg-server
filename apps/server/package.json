{
  "name": "@nogada/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @nogada/data build && tsx watch src/index.ts",
    "start": "pnpm --filter @nogada/data build && tsx src/index.ts",
    "migrate": "node-pg-migrate -m migrations"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@node-rs/argon2": "^2.0.2",
    "@nogada/data": "workspace:*",
    "@nogada/shared": "workspace:*",
    "fastify": "^5.1.0",
    "node-pg-migrate": "^9.0.0",
    "pg": "^8.23.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/pg": "^8.21.0",
    "tsx": "^4.19.0"
  }
}
