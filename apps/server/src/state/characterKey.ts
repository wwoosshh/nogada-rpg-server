/**
 * 캐릭터 키를 짓는 **한 곳**.
 *
 * v1 은 계정 하나에 캐릭터 하나라(설계 §2), 캐릭터 키를 따로 발급하지 않고 계정
 * 키를 그대로 쓴다. 그러면 "이 사람의 캐릭터"를 찾는 데 질의가 필요 없다 —
 * 세션이 계정 키를 주고, 그것이 곧 캐릭터 키다.
 *
 * 그래도 `characters.user_id UNIQUE` 를 따로 두는 이유는, 계정당 하나라는 규칙이
 * **이 함수의 성질**이 아니라 저장소의 제약이어야 하기 때문이다. 슬롯이 여럿이
 * 되는 날 바뀌는 것은 이 함수 하나이고(키 발급이 BIGSERIAL 로 옮겨 간다), 그때
 * 제약도 함께 풀린다 — 지금 제약이 없으면 그날까지 아무도 규칙을 지키지 않았다는
 * 것을 알 수 없다.
 */
export function characterIdOf(userId: string): string {
  return userId
}
