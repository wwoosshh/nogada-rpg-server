# 다음 콘텐츠 추천 — 컨텍스트를 공유하지 않는 세션 넷 (2026-08-13)

사용자 상시 지시: "하나의 작업이 끝났을 때에는 너의 컨텍스트에 영향받지 않는 세션으로
콘텐츠나 게임요소를 추천받아서 최대한 다양하고 개성있는 게임성을 갖도록 개발해."

제작 확장 아크가 끝난 시점에서 네 각도(장르 비교 · 원작 미구현 · 온라인성 ·
플레이어 경험)로 물었다. **셋 이상이 같은 곳을 가리킨 것이 하나 있다.**

---

## 최대 합의: 목록방(도감) — "추가가 아니라 복원"

| 각도 | 이름 | 비용 |
|---|---|---|
| 장르 | 목록방 — 잠긴 칸까지 보이는 도감 | 중간 |
| 원작 | **수집의 방(진열종합) + 채집물 보관소** | 작음 |
| 플레이어 | 사다리 도감 — 잠긴 칸까지 보이는 계열별 세로 목록 | 중간 |
| 온라인 | 마을 석비(최초 달성자) — 위 도감 위에 얹는 사회적 층 | 작음 |

**원작 근거(원작 미구현 렌즈가 발굴):** CE 541~545 `[천무]수집시스템`. 아이템마다
"10개당 몇 점"이 따로 박혀 있고(허브 10점·라벤더 12점·라임 15점·세이지 18점),
바치면 아이템은 **사라진다**("교환하면 아이템이 사라집니다"). 점수는 아이템별
변수(v201 허브점수 …)에 따로 쌓이면서 v197 진열종합에 합산되고, 바친 개수가
v202 허브진열 …에 **전시 수량**으로 남는다. CE 540 수집보상이 25/50/75/100/125/128
점에서 상점을 단계적으로 연다. 스위치 1256~1260 = 수집달인·고수·마스터·왕·신.
CE 542 는 인벤토리와 **보관소**(v317)를 함께 세어 바친다 — 보관소는 이 시스템의 부품.

**왜 지금인가:** ① 우리 메모리가 원작의 4대 장치 중 첫째로 적어 둔 것이
"잠긴 것까지 보이는 목록방"이다. ② 지금 진행도를 한눈에 보는 화면이 없다
(이정표 탭은 숫자 축만 말하고 물건 축의 사다리는 어디에도 없다). ③ 재료의
세 번째 용도(팔기·만들기 다음)가 생긴다. ④ 나중에 온라인 층(석비·최초 달성자)이
얹힐 그릇이 된다.

**지켜야 할 선(장르 렌즈의 경고):** 도감은 **개수만 말하고 숫자는 말하지 않는다.**
잠긴 칸에 요구치를 적는 순간 5,000 거래·85,000 결계 문턱의 값어치가 사라진다.

---

## 그 밖의 값진 후보 (다음 순번 후보로 보관)

**플레이어 경험 — 신규의 첫 30분이 비어 있다**
- **브라켓 경계 표시기**(작음): 실제로 무언가 바뀌는 순간은 채집 브라켓 경계인데
  이정표와 겹치는 것이 10,000 하나뿐이라 **다섯 순간 중 넷이 침묵**한다.
- **첫 연장 계약**(중간): 구리 도구 넷이 requiredSkill 0 인데 재료가 두 채집장에
  흩어져 있고 그 경로를 말해 주는 장치가 없다 — 탈출구가 5분 거리인데 맨손으로
  45분을 버틴다.
- **역참조 가방**(작음): "이 아이템이 어디에 쓰이는가"를 세 표에서 거꾸로 훑는
  순수 함수 하나. 신규가 구리 원석을 팔아 자기 길을 막는 사고를 없앤다.
- **복귀 브리핑**(중간): 3일 비우면 게임 시간 72일이 흐르는데 그 흔적이 0이다.

**온라인성 — 서버가 이미 아는 것만으로**
- **노드 흔적**(작음): "3시간 전, 세렌이 여기서 얼음의보석을 캤다" — 웹소켓 0.
- **world.\* 사실**(작음): NPC 가 남을 입에 담는다("네 나무 솜씨가 이 근방에서
  셋째다"). 새 화면 0개로 랭킹을 대사로 전달.
- **공동 결계**(중간): 이미 심어 둔 85,000 소문의 주어를 "너"에서 "우리"로.

**원작 미구현 — 큰 것들**
- **평가숙련도 한 숫자 + 명예의 증표 5단**(작음): 원작 CE 656 이 모든 숙련을
  가중 합산해 v1063 을 만들고, 1,000/5,000/10,000/30,000/100,000 에서 훈장을 준다.
  랭킹·업적을 새 콘텐츠 없이 동시에 채운다.
- **노점(장사)**(중간): 8칸 진열·값은 내가 매김·57분 부패·30분마다 NPC 가 사감.
  플레이어 간 거래의 그릇이 된다(손님 자리에 사람을 넣으면 끝).
- **오크 동료**(큼): 원작에서 가장 배선이 많은 시스템. 옆에서 같이 캔다.

**장르 표준 — 결정을 미루면 비싸지는 것**
- **인벤토리 상한 + 창고**(큼): 지금 stacks 가 무한이라 채집 루프에 마침표가 없고
  마을↔채집장 왕복이 없다. **넣을지 말지는 지금 정하는 게 싸다** — 무한 소지를
  전제한 기능을 더 쌓은 뒤에 얹으면 이행 비용이 배가 된다.
- **자리비움 수확**(중간): 최하 티어만·숙련 없음·8시간 상한이면 손 채집의 값어치를
  오히려 올린다. 다만 되돌리기 거의 불가능한 결정.
- **떠돌이 행상**(작음): 오늘 지목한 3품목만 정가로 사간다 — 시각이 처음으로
  플레이어의 행동을 바꾼다.

---

## 결정

다음 아크는 **수집의 방(도감 + 헌납)** — 합의가 가장 크고, 원작의 첫째 장치이며,
진행도 화면의 부재를 함께 메우고, 온라인 층이 나중에 얹힐 그릇이 된다.

---

# 2라운드 (2026-08-13, 수집의 방 아크 종료 후)

컨텍스트를 공유하지 않는 세션 셋에게 다시 물었다. **1라운드와 달리 수렴이 없었다** —
셋이 서로 다른 층을 가리켰고, 셋 다 근거가 튼튼했다.

| 각도 | 추천 | 크기 | 핵심 근거 |
|---|---|---|---|
| 플레이어 경험 | **85,000 결계** — 조건부 전환 + 심층 구역 | 중간 | 중반 **179분**이 사건 없이 비어 있고 85,000 이 그 한복판(구리 154분). 네 NPC 가 이미 그 문을 말한다. 심층 노드 12개가 `tableId` 를 공유해 **표시 전용 거짓말**이다 |
| 온라인성 | **곁두레** — 같은 채집장의 손을 서로 본다 | 중간 | 저장소에 **교차 캐릭터 읽기가 한 줄도 없다**(`WHERE id = $1` 뿐). `updated_at` 은 모든 행동에서 이미 찍히는데 읽는 코드가 없다. 몸(x·y) 대신 **노드**를 앵커로 — x·y 는 맵 전환 때만 갱신되므로 아바타를 세우면 전원이 입구에 모인다 |
| 원작 미구현 | **씨앗과 밭** — 시간이 재료를 만드는 첫 손 | 중간 | 원작 집의 방 셋 중 우리는 수집의방만 지었다(map157/158/**160**). `CE 280` 씨앗 8종·별도 숙련·**한 번 캐면 사라짐**·야외 108군데. `CE 279` 10분 틱. 이 게임의 faucet 은 손 하나뿐이고 sink 는 이제 셋이다 |

## 조사 중 드러난 것 — 85,000 은 원작의 게이트가 아니었다

`v11 = 랜덤`, `v42~45 = 계열별 채집숙련도`. 세 "상징 숫자" 중:
- **5,000 거래** 진짜(`CE 271~274`: `v42~45 ≥ 5000`)
- **63,235 달인** 진짜(`v42 ≥ 63235`)
- **85,000** 은 `CE 259` 의 `랜덤 ≤ 85000` — **누적 확률표의 마지막 문턱**(그 위는 꽝)

채집 티어 스펙 §6-앞 14 에 정정을 적었다. **결계는 원작 복원이 아니라 우리가 한 약속이다.**

## 결정: 결계 (+ 되사기 문 재설계)

원작 근거가 사라졌는데도 결계를 고르는 이유:

1. **출하된 대사 넷이 없는 것을 가리키고 있다.** 네 마을 주민 전원이 같은 문을 말하는데
   그 문이 없다. 이것은 빠진 기능이 아니라 게임 안에 든 거짓말이고, 다른 둘에는 없는 성격이다.
   심층 노드 12개(이름·마커 색이 다른데 표가 같다)가 같은 거짓말의 두 번째 자리다.
2. **다른 둘은 더하고, 이것은 이미 있는 것을 작동시킨다.** 미스릴 도구 원재료 25.0시간·
   수집의 방 2등급 48.0시간이 중반의 유일한 목표인데 그 구간의 손으로는 닿지 않는다.
3. **온라인 층이 이 위에 얹힌다** — 1라운드의 "공동 결계"는 결계가 있어야 성립한다.
4. **밭은 사용자 결정을 기다린다.** 자리비움/시간 생산은 이 저장소가 구조적으로 피해 온
   축이고(`weather.ts` 가 "남은 시간을 적으면 그것을 줄이는 주체가 필요해진다"고 명시),
   1라운드도 "되돌리기 거의 불가능한 결정"이라 적었다. **게임이 무엇인가를 바꾸는 결정은
   사용자 부재중에 내리지 않는다.** 곁두레와 함께 다음 순번으로 보관한다.

되사기 문 재설계(수집의 방 §9 — 골드로 수집 점수를 사는 간선)를 같은 아크에 묶는다:
둘 다 **문**에 관한 것이고, 되사기 자물쇠를 옮기는 일이 결계의 게이트 문법과 같은 자리다.

## 보관 (다음 순번)

- **곁두레**(온라인) — `listActive(sinceMs)` 한 메서드 + `updated_at` 인덱스 한 줄. 새 화면 0.
- **씨앗과 밭**(원작) — 사용자에게 자리비움 생산 여부를 먼저 묻는다.
- **∞ 브라켓 축하**(작음) — 숙련 500,000 에서 최상위 티어가 230배 뛰는데 이정표도 대사도 없다. 결계 아크에 곁줄로 얹을 수 있다.
- **가축의 방·낚시·노점·보관소** — 산출물 용도가 없거나(가축·낚시), 사람이 둘 이상이어야 값이 생기거나(노점), 루프가 아니라 제약이다(보관소).
- 1라운드 보관분은 위 절에 그대로 유효하다.

---

# 사용자 지목 (2026-08-13) — 노드의 얼굴과 종류

> "현재 채집노드들도 너무 원시적으로 박스로 만들어져있는데 디자인 에셋을 가졌으면 좋겠어.
> 원작에서는 하위노드들로 채집을 시작하고 상위노드도 존재하는등으로 구현이 되었어.
> 각각은 특징들을 가지고 있어. 예를들어 원작의 뜨거운 얼음은 특수한 빨간얼음노드에서만
> 채집이 되는식으로 말이야. 물론 제대로 채집하기 위해서는 숙련도가 필요하지만"

**결계 아크 다음 아크로 확정.** 조사 결과는 아래.

## 진단 — 에셋이 없는 게 아니라 안 물렸다

`apps/client/src/game/NodeMarker.ts:37` 이 말 그대로 `scene.add.rectangle(0, 0, 24, 24, 색)` 이다.
색은 둘뿐(`normal 0xa8785a` / `deep 0x4d8a99`).

그런데 이 저장소에는 **이미 라이선스된 에셋 팩이 있다**(`assets/CREDITS.md`):
- Pipoya FREE RPG Tileset 32×32 — 맵 타일 (상업 ✅ · 엔진제한 없음 · 편집 ✅)
- finalbossblues 1000+ Fantasy RPG Icons ($6) — 아이템 아이콘 64개가 여기서 나왔다
- Pipoya character sprites — 플레이어·NPC, 그리고 **`sign_wood.png`(안내판)까지**

아이템도 NPC도 안내판도 그림을 갖는데 **노드만 도형이다.** 에셋 부족이 아니라 배선 누락이다.
(에셋 실물은 `assets/licensed/` 에 있고 재배포 금지 조항 때문에 git 밖이다.)

## 노드 종류 — 배관은 이미 있다

`nodes.csv` 의 각 노드가 `tableId` 를 가리키고, **결계 아크 B2 가 그 기계를 실제로 돌렸다**
(심층 표 넷 신설). 그러므로 "빨간 얼음 노드에서만 뜨거운 얼음"은 새 구조가 아니라
**`nodes.csv` 한 줄 + `gather_tables/tiers/brackets` 한 벌 + `items.csv` 한 줄 + 아이콘 하나 + 배치**다.

**원작도 같은 모양이었다**(확인함): 채집 커먼이벤트가 노드 색깔별로 갈린다 —
`[♣채]광보라잘잘`(CE 264) · `광녹색잘못`(265) · `광적색못못`(266) · `광청색못잘`(267),
그리고 특수 노드 `빛깔결정`(268) · `생명의나무`(270) · `천년허브`(275).
CE 266 을 열어 보니 **우리와 같은 구조**다 — `v44`(광물숙련) 브라켓 → `v11`(랜덤) 누적 문턱.

## 숙련도와 규범 — 충돌하지 않는다

이 저장소의 규범은 "**노드는 잠기지 않고, 숙련은 접근이 아니라 분포를 바꾼다**"
(`milestones.ts:31-33`). 사용자가 말한 "제대로 채집하려면 숙련도가 필요하다"는 그 규범과
**같은 말**이다 — 빨간 얼음 노드는 저숙련자를 거절하지 않고, 그 표의 낮은 브라켓에서는
잡것만 주다가 숙련이 오르며 특수 재료가 나오기 시작한다. 문 앞에서 쫓겨나지 않으면서
"아직 이르다"가 손에 잡힌다.

## 이 아크가 답해야 할 것

1. **노드가 그림을 갖는다** — 계열 × 등급(하위/상위/특수)마다 다른 스프라이트. 승인 절차는
   기존 관례대로(컨트롤러가 4배 대조 시트로 승인 — `2026-08-12-gathering-tiers.md` G1 선례).
2. **특수 노드 종류를 몇으로 할 것인가** — 계열마다 특수 하나? 넷 전부? 그리고 특수 재료의
   **용도**가 있어야 한다([[systems-must-close-loops]]) — 팔기·바치기만으로는 값이 안 선다.
   지금 최상위 넷 중 `golden_fruit`·`millennium_leaf` 는 이미 어떤 레시피에도 안 들어간다.
3. **배치** — 특수 노드가 결계 안인가 밖인가. 결계 아크가 만든 심층 구역과 어떻게 겹치는가.
4. **원작을 권위로 쓰지 않는다**([[original-is-not-authority]]) — 색 네 가지·특수 셋은 원작의
   구현이고, 우리가 몇을 둘지는 플레이어에게 재미있는가로 따로 판정한다.
