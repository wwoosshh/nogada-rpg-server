{
  "name": "nogada-rpg-fanmade",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20.10.0" },
  "scripts": {
    "test": "pnpm data:build && vitest run",
    "test:watch": "pnpm data:build && vitest",
    "typecheck": "pnpm data:build && tsc --build && pnpm --filter @nogada/client typecheck",
    "data:build": "pnpm --filter @nogada/data build",
    "dev:server": "pnpm data:build && pnpm --filter @nogada/server dev",
    "dev:client": "pnpm data:build && pnpm --filter @nogada/client dev"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
